# Vanillang 模块

模块是 Vanillang 的基本组织单位，一个模块对应一个源文件，模块内可以定义函数、类、接口、枚举、类型别名等元素。模块可以通过导入其他模块来使用其定义的元素，也可以被其他模块导入以供使用。注意：只有使用 `export` 关键字导出的元素才可以被其他模块访问，未导出的元素只能在当前模块内使用。模块的名称由源文件的名称决定，例如 `utils.vnl` 模块的名称为 `utils`，模块名必须满足标识符的命名规则。